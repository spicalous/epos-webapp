{{#each categories as |category|}}
  <div class="col-xs-4 col-sm-3 col-md-2 no-padding">
    {{#btn/btn-selectable class="btn-default" model=category
        selected=selectedCategory
        action="selectCategory"}}
      <small>{{category.name}}</small>
    {{/btn/btn-selectable}}
  </div>
{{/each}}