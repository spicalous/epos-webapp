<div>ID {{order.id}}</div>
<div>Ordered at {{format-time order.dateTime}}</div>
<div>
  £{{format-currency order.total}} {{format-payment-method order.paymentMethod}}
</div>
{{#if order.notes}}
  <div>{{order.notes}}</div>
{{/if}}
<div>
  {{component (concat "order-" order.customer.customerType) customer=order.customer}}
</div>
<ul class="list-group">
  {{#each order.orderItems as |orderItem|}}
    <li class="list-group-item">
      {{orderItem.quantity}} x {{orderItem.menuItem.menuId}} {{orderItem.menuItem.name}}
    </li>
  {{/each}}
</ul>
{{#btn/btn-default class="btn-primary" action="printOrder"}}PRINT{{/btn/btn-default}}