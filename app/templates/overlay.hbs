<div class="overlay" {{action "dismissMessage" "overlay" model.callback}}>
  <div class="overlay-inner">
    <div class="text-center">
      <h2>
        {{model.header}}
      </h2>
      {{model.body}}
      <div class="text-muted">
        <small>Tap anywhere to dismiss</small>
      </div>
    </div>
  </div>
</div>