<div class="container-fluid">
  <div class="row gutter-top-bottom text-center">
    <h4>Order</h4>
    <div class="col-xs-12 gutter-top">
      {{#link-to "orderpad"}}
        {{#btn-button class="btn-main btn-home"}}NEW ORDER{{/btn-button}}
      {{/link-to}}
    </div>
  </div>
  <div class="row gutter-top-bottom text-center">
    <h4>Management</h4>
    <div class="col-xs-12 col-sm-4 gutter-top">
      {{#link-to "customers"}}
        {{#btn-button class="btn-main btn-home"}}CUSTOMERS{{/btn-button}}
      {{/link-to}}
    </div>
    <div class="col-xs-12 col-sm-4 gutter-top">
      {{#link-to "road"}}
        {{#btn-button class="btn-main btn-home"}}ROADS{{/btn-button}}
      {{/link-to}}
    </div>
    <div class="col-xs-12 col-sm-4 gutter-top">
      {{#link-to "postcode"}}
        {{#btn-button class="btn-main btn-home"}}POSTCODES{{/btn-button}}
      {{/link-to}}
    </div>
  </div>
</div>
<div class="text-muted" style="position:absolute;bottom:5px;left:10px;">
  {{name}} - {{version}}
</div>