<div class="container">
  <div class="text-muted" style="position:absolute;bottom:0;right:0;">
    {{name}} - {{version}}
  </div>
  <div class="row text-center">
    {{#link-to 'orderpad.restaurant'}}
      {{#main-btn}}RESTAURANT ORDER{{/main-btn}}
    {{/link-to}}
    {{#link-to 'orderpad.takeaway'}}
      {{#main-btn}}TAKEAWAY ORDER{{/main-btn}}
    {{/link-to}}
    {{#link-to 'orderpad.delivery'}}
      {{#main-btn}}DELIVERY ORDER{{/main-btn}}
    {{/link-to}}
  </div>
</div>